---
description: If the AI is unable to determine the missing components, ask the user.
---

Ask the user if they have any idea of what components might be missing in the backend. Also ask about the business logic of the backend to better identify the missing components.