// Proxy middleware para Next.js 16 (usando archivo proxy en su lugar)
// Este archivo se mantiene para compatibilidad pero no se usa
export function middleware() {
  return
}

export const config = {
  matcher: [],
}
